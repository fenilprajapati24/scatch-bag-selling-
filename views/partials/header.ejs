<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">

   <nav class="w-full fixed top-0 z-50 flex justify-between items-center px-8 py-3 bg-blue-600 border-b border-green-800 shadow-md text-white">
  <h3 class="text-2xl font-bold tracking-wide select-none">Scatch</h3>

  <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true; %>
  <% if (loggedin) { %>
    <div class="flex gap-8 text-lg font-semibold">
      <a href="/shop" class="hover:text-yellow-300 transition-colors duration-200">Shop</a>
      <a href="/cart" class="hover:text-yellow-300 transition-colors duration-200">Cart</a>
      <a href="/about us" class="hover:text-yellow-300 transition-colors duration-200">About Us</a>
      <a href="/users/logout" class="text-red-400 hover:text-red-600 transition-colors duration-200 font-bold">Logout</a>
    </div>
  <% } %>
</nav>
